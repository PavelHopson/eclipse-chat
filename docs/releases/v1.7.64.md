# Eclipse Chat v1.7.64 — workspace polish

Release approved by the user on 2026-08-27. Target: existing master branch and the
production environment at https://app.star-crm.ru/eclipse-chat/.

## Scope

- Eclipse pointer for fine mouse input; native text/media cursors and reduced-motion fallback.
- Single-surface profile/logout/search controls; existing logout callback and animation preserved.
- Phosphor regular icons with all 30 existing ec:* channel IDs preserved.
- Portalled workspace menu with bounded placement and keyboard navigation.
- One active rail surface; no clipped outer selection glow.
- Compact, responsive Platform Admin with protected-account guards and existing confirmations.
- Full-width training library, optional management controls, larger-video mode and delete confirmation.
- Version 1.7.64 in root/web/server manifests and service-worker cache.

## Release isolation

The local App.tsx preview route, WorkspacePreviewPage, preview fixtures/styles,
.codex-qa media, screenshots, logs and unrelated dirty files are not part of this
release commit. Production uses the existing authentication entry point.
The UI contract suite does not depend on any local preview file.

## Safety and deployment

The existing production environment approval stays required. CI now runs the UI
and release contracts in addition to existing server/typecheck/audit gates.
Before the existing checkout/build activation, the production SSH job creates
root-only, uniquely named custom-format PostgreSQL backups of eclipse_chat and
star_crm_prod, as required by DEPLOY-TO-STAR-CRM.md. Both must pass pg_restore --list.
The Star CRM target is checked against both its application and backup configuration.
The backup step checks the managed path, available memory and disk space. It does
not delete or rotate backups and does not print credentials or database contents.
An unavailable database, failed dump or failed validation stops the update.

Existing staged-build rollback and Nginx validation remain unchanged. No DNS,
database schema, roles, auth endpoints or production secrets are changed by this
release. Shared-host Star CRM receives only the documented read-only DB backup
and a post-deploy availability check; StarMarket is not a deployment target.

## Verification before publication

- Desktop QA at 1184x920, 1440x900 and 1904x943; mobile at 390x844.
- Menu keyboard/viewport behavior, admin confirmations, search empty state,
  library management/large mode and native media controls checked locally.
- Server suite: 71 files / 376 tests passed (local maxWorkers=2).
- UI and release contracts: 15 passed; security contracts: 15 passed.
- Workspace typecheck and web/server build passed.
- npm audit, including dev dependencies: zero reported vulnerabilities.
- Production bundle verified free of local preview markers and fixture content.
- Existing warning for boot-preferences.js (non-module script) remains unchanged.

This is a proportional release review, not a full production penetration test.
Post-deploy evidence is the exact commit's Deploy PROD run, /api/version,
release metadata, matching web assets and /api/health. Authenticated production
views require an existing user session and must not be simulated with fixtures.

## Production result — 2026-08-27

- Deployed application commit: 0d8ca0feb618e65978f57ca94e78014db2ca854e.
- [CI](https://github.com/PavelHopson/eclipse-chat/actions/runs/33110221256),
  [Security Gate](https://github.com/PavelHopson/eclipse-chat/actions/runs/33110221318)
  and [Deploy PROD](https://github.com/PavelHopson/eclipse-chat/actions/runs/33110221296): success.
- Verified backups: /var/backups/eclipse-chat/release-20260827T195107Z-eIIey7/
  contains eclipse_chat.dump (433646 bytes) and star_crm_prod.dump (796408687
  bytes), both mode 0600 under a root-only directory. Archive listing passed;
  no restore into a live database was attempted.
- /api/version and service worker report 1.7.64; /api/health reports ok:true and
  database:true. Supervisor, existing uploaded-image MIME smoke, AI live
  completion and external CI smoke passed. Star CRM root remains HTTP 200.
- Unauthenticated GET /api/platform/users returns HTTP 401.
- Production assets match the local release build byte-for-byte (SHA-256):
  - index-CR0iOlG6.js: 27A7926E3D9898D89A05331B8E855C596B7F8365503C60605E9B0CFAFB20A91B
  - AppShell-BfDUWpSH.js: 43D2F18061AD4BE5858176A09D56244265206C2F27A28C0571551774EF1BD8EE
  - AppShell-BhL_rDNw.css: ECF92C428927740EE41DEC88EBC80AA474AD46F63C7249D7AEFF3866F65CE813
- Production browser: login/registration at desktop 1280px and mobile 390px,
  no horizontal overflow, no broken images or console errors; password focus
  and registration/login switching work without submitting account data.
- Authenticated workspace/admin/video views were verified locally, not through
  a fabricated production session. The available browser has no signed-in
  production account, so the owner should review those views after login.

## Preflight incident and operational limitation

The first attempt (run 33109218001) stopped before checkout mutation: the old
guide named a nonexistent star_crm database. The Eclipse Chat dump was verified,
and production stayed healthy on v1.7.63. Read-only diagnostics (run 33109965583)
confirmed DB_DATABASE=PGDATABASE=star_crm_prod on localhost:5432; the preflight and
guide were corrected without skipping backups or changing application data.

Security review: no new Critical/High findings in the changed surface; dependency
audit reports zero advisories. Existing unresolved Medium risk: master has no
branch protection/ruleset. The production reviewer gate and CI are enabled, and
this release waited for both CI and Security Gate before approval. Repository
protection settings were not changed as part of this visual release.
