# Личные цвета — 2026-09-05

## Область и checkout

- `E:\projects\eclipse-chat`, ветка `master`, база `0346703`.
- Запрос: персональные цвета всего интерфейса; позднее уточнение — только тёмная основа, без переключателя светлой темы.
- Решения и контракт: [личное оформление](../design/personal-appearance.md).
- Отдельно от незавершённого [спринта надёжности v1.7.73](v1.7.73-reliability.md). Его изменения и пользовательские DEV-прототипы сохранены. Веб-тест звонка остановлен перед этой работой; новых проверок физических устройств здесь нет.

## Реализовано локально

- Редактор шести цветов, четыре палитры, предпросмотр, отмена и сохраняемый сброс; сообщение об ошибке/успехе, блокировка нечитаемых цветов.
- Личная таблица и GET/PUT API, кэш по аккаунту, защита от поздних ответов предыдущего аккаунта, обновление при фокусе без потери черновика.
- Переключение тем убрано; пункт меню профиля ведёт в оформление. Палитра имеет приоритет над брендом пространства.
- Исправлен конфликт адаптивной сетки настроек на промежуточной ширине и переполнение мобильного блока действий.

## Проверки

- `npm.cmd test`: 112/112 веб-контрактов и поведенческих проверок; сервер 435 passed / 6 skipped (77 файлов).
- Новая область: 13 веб-проверок (настоящие функции и hook с управляемым транспортом) + 26 серверных (Fastify inject, мок БД).
- `npm.cmd run typecheck`: веб и сервер прошли.
- `npm.cmd run build`: прошёл; прежнее предупреждение Vite о классическом `boot-preferences.js` сохраняется.
- После переноса глобальных стилей из lazy-настроек в AppShell повторены полный набор тестов и production build: оба прошли. `git diff --check` без ошибок (только предупреждения CRLF).
- Prisma client сгенерирован локально; первоначальный EPERM разрешён узкой эскалацией только для генерации клиента. Миграция БД не запускалась.
- Gitleaks 8.30.1, `stdin --redact`, шесть новых TS/TSX/MJS файлов, около 35 KB: 0 утечек.
- Браузерный стенд без реальных аккаунтов/бэкенда: 1280×720, 1100×850, 390×844, 320×740. Проверены предпросмотр, запрет плохого контраста, сохранение/перезагрузка, изоляция A/B, закрытие без сохранения, сбой сохранения, сброс, Tab и видимый фокус. После исправлений переполнения палитры и действий нет; горизонтальная прокрутка меню намеренная.
- Escape через инструмент в IAB не дал подтверждённого закрытия; не считать этот сценарий проверенным. Закрытие кнопкой и отмена проверены.
- Отдельно проверен ручной HEX `#ffa46b`: после сохранения и перезагрузки совпадают поле и применённый CSS-токен. Тестовый сервер остановлен, тестовая вкладка закрыта, временный viewport сброшен; продовая вкладка оставлена открытой.
- По навыкам XSS/API-security: строгий HEX на обеих границах, отсутствие HTML/URL sinks, JWT-only ownership, no-store, строгие поля/размер, rate limit. Новых Critical/High в этой области не выявлено. Ранее записанный риск localStorage-токенов не менялся этой задачей.

## Выпуск и следующий шаг

- Пользователь разрешил выпуск оформления сообщением «Хорошо, делаем» 2026-09-05, в ответ на план отдельного релиза оформления. Незавершённая надёжность и DEV-прототипы не входят в этот выпуск.
- Коммита, push, миграции и публикации пока нет. Версии манифестов не повышались.
- Нативная синхронизация и реальная БД пока не проверены: тестовый HTTP-стенд и мок БД не являются доказательством работы production-сценария.

## Предрелизная проверка — 2026-09-05

- Повторно подтверждены checkout `E:/projects/eclipse-chat`, ветка `master`, HEAD `0346703` и remote `PavelHopson/eclipse-chat`. Рабочие изменения сохранены; staging не менялся.
- `npm.cmd audit --audit-level=high --json` завершился с кодом 1: два уязвимых пакета (Critical 0, High 1, Medium 1, Low 0). `npm.cmd ls fastify fast-uri` подтверждает `fastify@5.10.0`, `fast-uri@3.1.5` и `fast-uri@4.1.2`.
- High: `fast-uri` имеет опубликованные дефекты нормализации адресов, включая SSRF при использовании результата в исходящих запросах. Наличие эксплуатируемого пути в Eclipse Chat пока не доказано; release audit всё равно блокирует выпуск. Исправленные ветки: 3.1.6 и 4.1.3. [Advisory владельца](https://github.com/fastify/fast-uri/security/advisories/GHSA-f65p-4m7j-42xc).
- Medium: `fastify` — schema primitive coercion и trustProxy hop-count advisories, исправление 5.12.1. [Advisory владельца](https://github.com/fastify/fastify/security/advisories/GHSA-w2qp-rph6-63g4). В этом проходе зависимости ещё не обновлялись; findings остаются открытыми.
- Применены API Security Testing (проверка ownership/валидации нового маршрута) и SBOM/supply-chain подход (фактическое дерево npm и актуальные advisories). Прочитанный Terraform-навык не применялся: Terraform в релизной области нет.
- Обнаружена граница полномочий: текущий `.github/workflows/deploy-prod.yml` перед Chat deploy читает настройки и резервирует также `star_crm_prod`; `deploy/scripts/deploy.sh` дополнительно вызывает синхронизацию AI gateway. Эти шаги не запускались. Нужно согласовать ограничение релизного пути Eclipse Chat без изменения/резервирования другого продукта; нельзя молча удалить контроль резервного копирования или расширить выпуск на StarCRM.
- `git ls-remote` и `gh run list` не получили доступ к GitHub из sandbox (сетевые ограничения). Это не доказательство удалённого состояния; при продолжении запросить узкую сетевую эскалацию и перепроверить remote/gate. Auto-review отказов не было.
- Следующее безопасное действие после уточнения границы CI/CD: обновить уязвимые зависимости совместимыми версиями с регрессионными тестами, подготовить изолированный release diff и обязательные appearance-тесты в CI, повторить проверки, затем штатный backup/migration/deploy только согласованной области. После выпуска проверить реальную БД и сохранение палитры между вебом и Windows-приложением.

## Разрешённая подготовка v1.7.73 — 2026-09-05

- Пользователь ответил «Да» на согласование обновления зависимостей и изоляции release pipeline. `origin/master` повторно подтверждён через Git и GitHub; исходный commit — `0346703`.
- Обновлены только существующие зависимости: Fastify 5.12.3, fast-uri 3.1.7/4.1.4 и требуемый process-warning 5.1.0. Строгий lockfile; новых библиотек нет.
- Обычный Chat deploy резервирует только `eclipse_chat`; предварительно проверяет владельца/права конфигурации и точный локальный DB target без вывода реквизитов. Проверка свободного места, `pg_restore --list`, exact SHA, approval, migration и rollback сборки сохранены.
- Автопровизионирование AI gateway и Office secrets из обычного выпуска исключено. Отдельные provision/rotation/canary scripts и их защитные тесты сохранены. Nginx ограничен двумя Chat snippets, Supervisor — `eclipse-chat-server`; удаление общих nginx-бэкапов исключено.
- Версия server/web/root/lockfile/SW: 1.7.73. Desktop остаётся 1.0.9: его `frontendDist` уже указывает на production web URL. Нативный артефакт не меняется.
- Из Git index экспортирован точный релиз в `E:/projects/eclipse-chat-release-appearance-20260905` (проверочная копия без новой ветки и без пользовательских DEV-превью). Изменения звонков, root App.tsx/updateRecovery, Agents.md, design-qa и прочие прототипы не staged.
- Чистая установка `npm ci --ignore-scripts` + Prisma generate успешны. В этой копии: 105/105 веб-контрактов, 435 серверных passed / 6 skipped, 22/22 security-контракта; `npm audit --audit-level=high`: 0 уязвимостей. SBOM CycloneDX: 332 компонента / 333 записи зависимостей.
- Регрессионные тесты подтверждают отбрасывание malformed IPv6 в обеих установленных ветках fast-uri; совпадение реально загруженных версий с lockfile. В основной рабочей папке ранее остались старые файлы node_modules при обновлённом npm metadata; там два новых теста правильно упали. Успешную проверку чистой копии не смешивать с этим локальным расхождением.
- Обновлены два старых теста, которые требовали автопровизионирование интеграций: теперь они закрепляют отсутствие таких вызовов и неизменность текущего Chat environment. Защитные тесты отдельных provisioner-скриптов не удалялись.
- Gitleaks 8.30.1 по staged diff (~106 KB): 0 утечек. Синтаксис трёх изменённых shell scripts проверен `bash -n`, `git diff --cached --check` успешен.
- High/Medium dependency findings исправлены в релизном составе, но runtime-закрытие требует публикации. Неизменённый Medium-риск web refresh-токенов в localStorage остаётся вне этого слайса.
- Typecheck и production build чистого релиза с `VITE_BASE_PATH=/eclipse-chat/` успешны. Следующий шаг: commit/push в существующий master, успешные CI/Security Gate, approval production, доказательства backup/migration/version/health и проверка UI сохранения палитры.
