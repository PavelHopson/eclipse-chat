# v1.7.65 — release hold / CodeQL triage, 2026-08-28

UI release commit: 45f720917d07f094d837bac3a343258e9d2f9356, pushed to master.
Production remains v1.7.64. Deploy run 33168386839 is waiting for approval.
No production approval was sent. No protection rule or CodeQL alert was changed.

## Why the release is held

CI, deploy validate and Security Gate jobs are green, but the CodeQL alerts API
reports 24 open alerts for master: 20 labelled High and 4 Medium. All were first
seen on 2026-08-03, except #37 on 2026-08-24. None of the flagged server/deploy
files changed between 6da1c08 and 45f7209. These are not new UI regressions, but
the project's release rule does not allow silently treating an analysis job's
success as closure of unresolved High findings.

No new confirmed Critical vulnerability was found in this bounded review.
Severity labels below distinguish scanner output from verified behavior.

## Findings and evidence

| Alerts | Scanner label | Bounded assessment |
| --- | --- | --- |
| #37, configure-office-ingest.mjs:112 | High: file-system race | lstat validates a path, then readFile opens it again. The check/use gap is real; exploitability depends on write access to the parent directory. Parent ownership was not verified: the existing local SSH identity was denied, without reading any secret content or trying additional credentials. Keep unresolved; use descriptor-based no-follow reading and focused regression coverage. |
| #16, temporaryPassword.ts:14 | High: biased crypto random | Confirmed modulo bias: a 56-character alphabet maps 256 byte values to symbols with 4 or 5 occurrences. Sixteen symbols still provide over 90 bits of min-entropy, so this is not evidence of predictable passwords. Bounded assessment: Low hardening issue; replace modulo sampling with crypto.randomInt without changing the password format. |
| #17, twoFactor.ts:115 | High: biased crypto random | The actual alphabet has 32 symbols; 256 is exactly divisible by 32. Enumerating every byte value yields 8 occurrences per symbol. This specific modulo-bias claim is not reproduced. No change/dismissal made. |
| #15 / #18, bot generator and its test | High: biased crypto random | The actual alphabet has 64 symbols; each receives exactly 4 of 256 possible byte values. This specific bias claim is not reproduced. No change/dismissal made. |
| #11–14, uploads / emoji / server images | High: path injection | Filenames are built from identifiers that first pass membership/database checks; training filenames additionally pass basename + character filtering. Server creation does not accept a client-supplied id. These guards reduce the apparent dataflow risk, but no final blanket false-positive disposition is claimed; add explicit containment/identifier regressions before closure. |
| #19, linkPreview.ts:224 | High: double unescaping | Sequential replacements can decode nested entities. LinkEmbedCard renders title/description as React text, not HTML, so an XSS sink was not found in the reviewed consumer. Keep the parser behavior documented and cover nested entities before final disposition. |
| #1–10, docs/Эффекты крутые/Interactive Galaxy | High: insecure random | Documentation/reference script, not imported into the application release bundle. No authentication token use in shipped code was established by this alert. |
| #33–36, reference HTML documents | Medium: CDN without integrity | Documentation-only references, not production entry points or copied UI code. No new dependency/import was introduced. |

Existing Medium repository risk: master is unprotected and has zero rulesets.
The production required-reviewer gate remains present (environment 15291822396).
No setting was changed as part of the UI release.

## Bounded follow-up requiring scope confirmation

1. Harden the managed env reader against symlink/check-use races, with tests.
2. Remove modulo bias from temporary-password generation without changing length,
   alphabet, hashing, auth flows or existing credentials.
3. Add focused path-containment / invalid-id and nested-entity regressions, finish
   the remaining scanner triage with evidence rather than suppressing findings.
4. Rerun server, UI, release and security checks. Only then approve the exact
   production run, verify backups, deployment SHA, assets, version and health.

This is not an authorization to mutate production data, rotate credentials,
alter branch protection, dismiss scanner alerts, or bypass the approval gate.
No authenticated production attack tests were run.
