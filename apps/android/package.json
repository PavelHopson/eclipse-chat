{
  "name": "eclipse-chat-android",
  "version": "1.0.3",
  "private": true,
  "description": "Eclipse Chat — Android (Capacitor remote-load wrapper над прод-PWA)",
  "scripts": {
    "add": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android",
    "build:debug": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
